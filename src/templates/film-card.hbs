        {{#each this}}
        <li class="film item" data-movieId={{id}}>
          <a href="" class="film__link link">
            <img src="{{poster_path}}"  class="film__img" alt="">
            <div class="film__content"}>
              <h3 class="film__title">{{title}}</h3>
              <div class="film__info">
                <ul class="film__genres" >
                  {{#each genres}}
                  {{#if @last}} <li class="film__list__genre"> {{name}} </li>
                  {{else}}
                                <li class="film__list__genre"> {{name}}, </li>
                  {{/if}}
                 {{/each}}</ul> | {{release_date}}</div>
            </div>
          </a>
        </li>
        {{/each}}